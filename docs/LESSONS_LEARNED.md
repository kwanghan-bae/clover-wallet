# 🧠 Lessons Learned: Clover Wallet

이 문서는 AI가 작업을 수행하며 발견한 이 프로젝트만의 특이점, 가드의 한계, 혹은 새로운 효율적 코딩 패턴을 기록하는 공간입니다.

## 1. 기술적 통찰 (Technical Insights)
- **Expo Web 호환성**: `react-native-maps`와 같은 네이티브 라이브러리는 웹 빌드(`npx expo export`) 시 충돌을 일으키므로, `.web.tsx`를 통한 Mocking이나 플랫폼별 분기 처리가 필수적임.
- **Babel 플러그인 의존성**: Reanimated v4와 같은 최신 라이브러리는 `react-native-worklets`와 같은 피어 의존성을 수동으로 맞춰주어야 함.

## 2. 규약 및 가드 개선점 (Guard & Rules Improvements)
- **가드 유연성 확보**: 마크다운 문서(.md) 내의 자유로운 표현(`...`, `TODO`)이 커밋을 방해하지 않도록 `pre_commit.sh`에서 파일 확장자별 검사 로직을 분리함.
- **문서 동기화**: 로직 수정 시 문서 수정을 강제하는 규칙은 엄격히 유지하되, 작업의 긴급도를 고려하여 경고(Warning) 수준으로 완화하여 생산성을 확보함.

## 3. 회고 (Retrospective)
- **AI 환각 사례**: 문서를 단일화하거나 최적화하려는 과정에서, 원본의 풍부한 상세 지침을 '요약'이라는 명목하에 대량 누락시키는 과오를 범함.
- **방지 전략**: "효율성"보다 "무결성"이 우선임. 앞으로는 Git 히스토리의 원본 텍스트를 최우선 기준으로 삼고, 요약보다는 통합과 확장을 지향해야 함.

---

*이 문서에 기록된 핵심 내용은 주기적으로 `docs/init`의 전역 템플릿으로 전파되어야 합니다.*